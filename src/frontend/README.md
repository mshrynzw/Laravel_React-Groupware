
# Calming Groupware UI Design

Run `npm i` to install the dependencies.

Create `.env` and set backend API URL:

`VITE_API_BASE_URL=http://127.0.0.1:8000`

Run `npm run dev` to start the development server.
  